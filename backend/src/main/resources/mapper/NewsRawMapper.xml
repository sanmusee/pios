<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.pios.mapper.NewsRawMapper">

    <insert id="insertBatch" useGeneratedKeys="true" keyProperty="id">
        INSERT IGNORE INTO news_raw (title, content, source, publish_time, category, url, fetch_time)
        VALUES
        <foreach collection="list" item="item" separator=",">
            (#{item.title}, #{item.content}, #{item.source}, #{item.publishTime}, #{item.category}, #{item.url}, #{item.fetchTime})
        </foreach>
    </insert>

</mapper>
