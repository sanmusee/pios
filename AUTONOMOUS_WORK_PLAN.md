# PIOS 自主长时间工作执行方案

## 概述

本文档描述如何让 AI 助手根据 PIOS_SPEC.md 进行连续长时间工作（10+小时），无需人工介入。

## 核心机制

### 1. 任务分解与状态追踪

系统将所有工作分解为独立、可验证的任务单元，每个任务包含：
- **任务ID**: 唯一标识
- **依赖关系**: 前置任务
- **验证标准**: 完成判定条件
- **预计时间**: 执行时长估算
- **输出产物**: 生成的文件/代码

### 2. 自主执行循环

```
while (还有未完成任务) {
    1. 读取当前状态
    2. 选择下一个可执行任务（满足依赖）
    3. 执行任务
    4. 验证执行结果
    5. 更新状态
    6. 记录日志
    7. 如果遇到问题，尝试自动修复或标记阻塞
}
```

### 3. 容错与恢复

- **检查点机制**: 每完成一个任务自动保存状态
- **错误恢复**: 遇到错误时记录并尝试替代方案
- **状态持久化**: 所有状态保存在 `.pios_state.json`

## 使用方法

### 方式一：使用执行脚本（推荐）

```bash
# 1. 初始化项目
./pios-work.sh init

# 2. 检查环境依赖
./pios-work.sh check

# 3. 开始自主工作
./pios-work.sh start

# 4. 查看进度
./pios-work.sh status

# 5. 标记任务完成（由 AI 自动调用）
./pios-work.sh complete <task_name>
```

### 方式二：AI 自主执行指令

你可以直接告诉 AI：

> "请根据 PIOS_SPEC.md 开始自主执行，按照以下规则：
> 
> 1. 每次完成一个独立模块后立即保存状态
> 2. 如果遇到编译错误，尝试自动修复
> 3. 每完成 5 个任务汇报一次进度
> 4. 持续工作直到所有任务完成"

## 任务分解详情

### Phase 1: 项目初始化 (预计 30 分钟)

**Task 1.1: 创建目录结构**
- 创建 backend/ 和 frontend/ 目录
- 创建标准 Maven 和 npm 项目结构
- 验证: 目录结构符合规范

**Task 1.2: 后端基础配置**
- 创建 pom.xml (Spring Boot 3.x)
- 配置 application.yml
- 创建主启动类 PiosApplication.java
- 验证: `mvn clean compile` 成功

**Task 1.3: 前端基础配置**
- 创建 package.json (Vue 3 + Vite)
- 配置 vite.config.js
- 创建 main.js 和 App.vue
- 验证: `npm install && npm run build` 成功

### Phase 2: 后端核心实现 (预计 4-6 小时)

**Task 2.1: 数据库设计与实体类**
- 设计五层模型对应的数据库表
- 创建 JPA/MyBatis 实体类
- 创建数据库迁移脚本 (Flyway)
- 验证: 实体类编译通过

**Task 2.2: 第一层 - 信息抓取层 (Raw Layer)**
- 创建数据采集服务接口
- 实现宏观经济数据抓取
- 实现资本市场数据抓取
- 实现政策与科技信息抓取
- 验证: 服务层单元测试通过

**Task 2.3: 第二层 - 重要性判断层 (Filter Layer)**
- 实现信号分级算法 (Level 1/2/3)
- 创建重要性评估服务
- 实现规则引擎或评分模型
- 验证: 分级逻辑测试通过

**Task 2.4: 第三层 - 关联分析层 (Correlation Layer)**
- 创建因果链分析引擎
- 实现变量关联图谱构建
- 实现历史对比分析
- 验证: 关联分析测试用例通过

**Task 2.5: 第四层 - 推演层 (Inference Layer)**
- 实现三层推演算法
- 创建推演结果存储结构
- 实现置信度评估
- 验证: 推演逻辑正确

**Task 2.6: 第五层 - 汇报层 (Report Layer)**
- 实现日报生成服务
- 创建简报模板引擎
- 实现资产影响矩阵计算
- 验证: 生成的简报符合格式

**Task 2.7: RESTful API 实现**
- 创建所有 Controller
- 实现统一响应格式
- 添加 Swagger/OpenAPI 文档
- 验证: API 测试通过

### Phase 3: 前端实现 (预计 3-4 小时)

**Task 3.1: UI 框架搭建**
- 配置 Element Plus 或 Ant Design Vue
- 创建布局组件 (Header/Sidebar/Main)
- 配置路由 (Vue Router)
- 验证: 页面正常渲染

**Task 3.2: 状态管理**
- 配置 Pinia Store
- 创建五层模型对应的状态模块
- 实现 API 调用封装 (Axios)
- 验证: 状态管理正常工作

**Task 3.3: 第一层界面 - 信息采集面板**
- 创建数据源配置界面
- 实现采集任务管理
- 创建数据展示表格
- 验证: 与后端 API 联调成功

**Task 3.4: 第二层界面 - 信号分级面板**
- 创建信号列表视图
- 实现分级筛选功能
- 添加信号详情弹窗
- 验证: 分级显示正确

**Task 3.5: 第三层界面 - 关联分析面板**
- 实现因果链可视化
- 创建关联图谱展示
- 添加历史对比视图
- 验证: 图谱渲染正常

**Task 3.6: 第四层界面 - 推演面板**
- 创建推演结果展示
- 实现时间尺度筛选
- 添加置信度指示器
- 验证: 推演数据正确显示

**Task 3.7: 第五层界面 - 日报面板**
- 实现日报列表
- 创建日报详情视图
- 添加导出功能
- 验证: 日报生成和显示正常

### Phase 4: 集成与测试 (预计 2-3 小时)

**Task 4.1: 前后端联调**
- 配置 CORS
- 测试所有 API 端点
- 验证数据流完整性
- 修复联调问题

**Task 4.2: 数据库集成测试**
- 测试数据持久化
- 验证迁移脚本
- 测试数据查询性能
- 优化慢查询

**Task 4.3: 端到端测试**
- 创建完整工作流测试
- 测试数据采集到日报生成全流程
- 验证系统稳定性
- 记录性能基准

### Phase 5: 部署配置 (预计 1-2 小时)

**Task 5.1: 生产环境配置**
- 创建 application-prod.yml
- 配置日志输出
- 设置健康检查端点
- 验证: 生产配置正确

**Task 5.2: 构建脚本**
- 创建后端打包脚本
- 创建前端构建脚本
- 创建一体化构建脚本
- 验证: 构建产物正确

**Task 5.3: Docker 配置（可选）**
- 创建 Dockerfile
- 创建 docker-compose.yml
- 验证: 容器化部署成功

## 执行指令模板

### 启动自主工作

```
请开始执行 PIOS 项目开发。执行规则：

1. 首先运行 ./pios-work.sh init 初始化状态
2. 按照 Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 的顺序执行
3. 每个 Task 完成后：
   - 运行 ./pios-work.sh complete <task_id> 标记完成
   - 验证输出产物
   - 记录执行日志
4. 如果遇到错误：
   - 尝试自动修复（最多 3 次）
   - 如果无法修复，记录详细错误信息并继续下一个独立任务
5. 每完成一个 Phase 后汇报进度
6. 持续工作直到所有任务完成或遇到无法逾越的阻塞

请现在开始执行 Phase 1。
```

### 恢复工作

```
请恢复 PIOS 项目开发。执行步骤：

1. 运行 ./pios-work.sh status 查看当前进度
2. 从上次未完成的任务继续
3. 遵循相同的执行规则
```

## 状态文件说明

`.pios_state.json` 包含：
- `current_phase`: 当前执行阶段
- `completed_tasks`: 已完成任务列表
- `pending_tasks`: 待完成任务列表
- `current_task`: 当前正在执行的任务
- `progress_percentage`: 总体进度百分比
- `errors`: 遇到的错误记录

## 日志文件

所有执行日志保存在 `logs/pios.log`，包含：
- 时间戳
- 日志级别 (INFO/WARN/ERROR)
- 执行的操作
- 执行结果

## 注意事项

1. **内存管理**: 长时间工作注意监控内存使用
2. **网络依赖**: 部分任务可能需要网络（如下载依赖）
3. **编译时间**: Java 项目编译可能需要较长时间
4. **测试验证**: 每个任务完成后必须验证
5. **增量保存**: 重要文件修改后立即保存

## 预期产出

完成所有任务后，项目应包含：

```
pios/
├── backend/                 # Spring Boot 后端
│   ├── src/main/java/...   # Java 源代码
│   ├── src/main/resources/ # 配置文件
│   ├── pom.xml
│   └── target/             # 构建产物
├── frontend/               # Vue 3 前端
│   ├── src/               # 源代码
│   ├── dist/              # 构建产物
│   └── package.json
├── logs/                  # 执行日志
├── .pios_state.json      # 状态文件
└── pios-work.sh          # 执行脚本
```
